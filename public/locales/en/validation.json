{
    "invalidFormat": "{{ field }} is invalid format",
    "letterSpace": "{{ field }} must contain only letters and spaces",
    "min": "{{ field }} must be at least {{ min }} characters long",
    "required": "{{ field }} is required"
}
